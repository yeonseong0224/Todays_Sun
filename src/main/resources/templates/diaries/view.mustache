
<!--<div class="container mt-4">-->
<!--    <h3>{{diary.mood}} {{diary.title}}</h3>-->
<!--    <div class="text-muted small">작성자: {{diary.memberName}}</div>-->
<!--    <p class="text-muted">{{diary.date}}</p>-->
<!--    <hr>-->
<!--    <p>{{diary.content}}</p>-->

<!--    {{#isAuthor}}-->
<!--        <div class="mt-3">-->
<!--            <a href="/diaries/{{diary.id}}/edit" class="btn btn-warning">✏️ 수정</a>-->
<!--            <a href="/diaries/{{diary.id}}/delete" class="btn btn-danger">🗑 삭제</a>-->
<!--        </div>-->
<!--    {{/isAuthor}}-->

<!--    <div class="mt-3">-->
<!--        <a href="/diaries/list" class="btn btn-secondary">🔙 목록으로</a>-->
<!--        <a href="/diaries/{{year}}/{{month}}" class="btn btn-outline-info">📅 캘린더 보기</a>-->
<!--    </div>-->
<!--</div>-->

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{diary.title}}</title>
    <link rel="stylesheet" href="/css/show.css">
</head>
<body>

<div class="container mt-4">
    <div class="diary-wrapper">

        <div class="diary-header">
            <h2 class="diary-title">
                {{diary.title}} <span>{{diary.mood}}</span>
            </h2>

            <div class="diary-meta">
                🗓 {{diary.date}}<br>
                ✍️ {{diary.memberName}}
            </div>
        </div>

        <div class="diary-content">
            {{diary.content}}
        </div>
    </div>

    <div class="mt-4 d-flex gap-2 flex-wrap">
        {{#isAuthor}}
            <a href="/diaries/{{diary.id}}/edit" class="btn btn-today">✏️ 수정</a>
            <a href="/diaries/{{diary.id}}/delete" class="btn btn-logout">🗑 삭제</a>
        {{/isAuthor}}
        <a href="/diaries/list" class="btn btn-gather">🔙 목록으로</a>
    </div>
</div>

</body>
</html>
